/*This header file includes all the necessary headers used in the .cpp files*/
#include <map>
#include <vector>
#include "string"
#include "iostream"

typedef std::string str;
typedef std::map<str, str> LUT_Type;
typedef std::vector<str> Keys_Values_type;